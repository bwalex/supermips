// NOTE: THIS MODULE IS AUTOGENERATED
//       DO NOT EDIT BY HAND!
module <%= module_name %>(
<% for s in in_signals %>
  input <% if not s[:type].nil? %><%= s[:type] %><% else %>[<%=s[:high]%>:<%=s[:low]%>]<%end%> <%=s[:name]%>,<% end %>

<% for s in out_signals %>
  output <% if not s[:type].nil? %><%= s[:type] %><% else %>reg [<%=s[:high]%>:<%=s[:low]%>]<%end%> <%=s[:name]%>,<% end %>

  input stall,
  input clock,
  input reset_n
);

  always_ff @(posedge clock, negedge reset_n) begin
    if (~reset_n) begin
    <% for s in out_signals %>
      <%=s[:name]%> <= 'b0;<% end %>
    end
    else if (~stall) begin
    <% out_signals.each_index do |idx| %>
      <%= out_signals[idx][:name] %> <= <%= in_signals[idx][:name] %>;<% end %>
    end
  end

endmodule
